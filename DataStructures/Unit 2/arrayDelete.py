import numpy as np # array library
newArray = np.array([1, 2, 3], dtype=int) # creating new array
print(newArray) # printing
newArray = np.delete(newArray, 1) # deleting value of 2
print(newArray) # printing new array